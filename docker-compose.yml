services:
  api:
    container_name: API
    image: valt1n/drathos-backend:latest
    build:
      context: .
    environment:
      - API_PORT=5001
      - MONGODB_URI=mongodb://mongo:27017/drathos
      - JWT_TOKEN=hide_this
    ports:
      - "8080:5001"
    depends_on:
      - mongodb

  mongodb:
    container_name: MongoDB
    image: mongo:4
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
